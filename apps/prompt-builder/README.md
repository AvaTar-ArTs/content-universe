# Prompt Builder starter

A framework-neutral browser starter for the CreativeOS authoring surface. It consumes the shared prompt manifest and workflow graph contracts. Replace the DOM renderer with React/Next.js when the product surface is ready.

The important boundary is the manifest, not the UI framework.
